
  # Add Pipelines Tab

  This is a code bundle for Add Pipelines Tab. The original project is available at https://www.figma.com/design/mCpabjEGiC4m90ylccVXR1/Add-Pipelines-Tab.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  